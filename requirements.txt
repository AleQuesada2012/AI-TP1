# Core notebook environment. Pin versions after validating them across the
# operating systems used by the team.
jupyterlab
matplotlib
nbconvert
numpy
# 3.0.6 is blocked by Windows 11 Smart App Control; 3.0.3 loads correctly.
pandas==3.0.3
torch
ipywidgets
# Hyperparameter calibration
optuna
# wandb
